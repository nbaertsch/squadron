# Squadron — Environment Variables
# Copy to .env and fill in your values: cp .env.example .env
# NEVER commit .env or *.pem files (both are in .gitignore)

# ── GitHub App Credentials ───────────────────────────────────────────
# Register a GitHub App at https://github.com/settings/apps
SQ_APP_ID_DEV=
SQ_APP_CLIENT_ID_DEV=
SQ_APP_CLIENT_SECRET_DEV=
SQ_INSTALLATION_ID_DEV=

# Private key — use ONE of these:
#   SQ_APP_PRIVATE_KEY_FILE  (local dev — path to .pem file)
#   SQ_APP_PRIVATE_KEY       (CI — raw PEM content)
SQ_APP_PRIVATE_KEY_FILE=squadron-dev.YYYY-MM-DD.private-key.pem

# ── Copilot SDK Auth ─────────────────────────────────────────────────
# Fine-grained PAT from a Copilot-licensed GitHub account.
# Locally you can skip this if you've run `copilot auth login`.
# In CI this is set from the SQ_COPILOT_TOKEN secret.
# COPILOT_GITHUB_TOKEN=github_pat_...

# ── E2E Test Target ─────────────────────────────────────────────────
E2E_TEST_OWNER=your-github-username
E2E_TEST_REPO=squadron-e2e-test
